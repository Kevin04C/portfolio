import React from 'react'

export const Technologie = ({name}) => {
  return (
    <span className="bg-slate-700 text-slate-50 font-semibold py-1 px-2 rounded-lg text-sm font-sembold">{name}</span>
  )
}
